# BelaBox_jetson_pip
A custom pipeline for BELABOX (NVIDIA Jetson) that receives an incoming RTMP stream and overlays it as a Picture-in-Picture (PiP) element directly onto the primary camera feed (e.g., Cam Link 1080p30)
